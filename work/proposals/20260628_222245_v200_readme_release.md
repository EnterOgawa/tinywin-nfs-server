# v2.0.0 README正式版整理 提案書

## 変更対象

| ファイル | 変更内容 |
|---|---|
| `README.md` | 公開利用者向けに、入手、インストール、基本的な使い方を先頭側へ追加 |
| `README.md` | 開発者向けのビルド手順を `開発者向けクイックスタート` として整理 |
| `README.md` | リリースチェックリストへの導線をテスト項目側へ移動 |
| `CHANGELOG.md` | `2.0.0 - 2026-06-28` として正式リリース日を確定 |

## 変更箇所

### `README.md`

```diff
+v2.0.0 は正式版です。インストーラーには Java runtime、管理ツール、Windows サービス、既定設定、運用ドキュメントを同梱しています。
+
+## 入手とインストール
+最新版は GitHub Releases から `TinyWinNfsSetup.exe` を取得してください。
+
+## 基本的な使い方
+1. 管理ツールの `共有` タブで、公開名と Windows 側フォルダを設定します。
+2. 必要に応じて、書込可否と許可クライアントを設定します。
+3. `保存して再起動` を実行してサービスへ反映します。
+4. クライアント側で `/export` などの公開名を mount します。
+5. 問題がある場合は、管理ツールの診断とログを確認します。
```

### `CHANGELOG.md`

```diff
-## 2.0.0 - 未リリース
+## 2.0.0 - 2026-06-28
```

## 確認方針

- `git diff --check`
- 署名済みインストーラーの `Get-AuthenticodeSignature` が `Valid` であること
- GitHub Release 作成時に SHA256 を release note へ記載すること
